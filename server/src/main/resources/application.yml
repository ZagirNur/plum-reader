spring:
  application:
    name: plum-reader
  datasource:
    url: jdbc:postgresql://localhost:5432/plum_reader
    username: plum
    password: plum
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false

server:
  port: 8080
